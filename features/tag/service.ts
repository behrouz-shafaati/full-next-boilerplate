import c_service from '@/lib/entity/core/service'

export default class tagService extends c_service {
  /**
   * constructor function for tagService.
   *
   * @remarks
   * This method is part of the tagService class extended of the main parent class baseService.
   *
   * @param model - tagSchema
   *
   * @beta
   */
  constructor(model: any) {
    super(model)
  }
}
