import c_service from '@/lib/entity/core/service'

export default class campaignService extends c_service {
  /**
   * constructor function for campaignService.
   *
   * @remarks
   * This method is part of the campaignService class extended of the main parent class baseService.
   *
   * @param model - campaignSchema
   *
   * @beta
   */
  constructor(model: any) {
    super(model)
  }
}
