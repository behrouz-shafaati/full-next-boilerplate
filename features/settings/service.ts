import c_service from '@/lib/entity/core/service'

export default class settingsService extends c_service {
  /**
   * constructor function for settingsService.
   *
   * @remarks
   * This method is part of the settingsService class extended of the main parent class baseService.
   *
   * @param model - settingsSchema
   *
   * @beta
   */
  constructor(model: any) {
    super(model)
  }
}
