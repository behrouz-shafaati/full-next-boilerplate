import c_service from '@/lib/entity/core/service'

export default class menuService extends c_service {
  /**
   * constructor function for menuService.
   *
   * @remarks
   * This method is part of the menuService class extended of the main parent class baseService.
   *
   * @param model - menuSchema
   *
   * @beta
   */
  constructor(model: any) {
    super(model)
  }
}
