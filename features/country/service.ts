import c_service from '@/lib/entity/core/service';

export default class countryService extends c_service {
  /**
   * constructor function for countryService.
   *
   * @remarks
   * This method is part of the countryService class extended of the main parent class baseService.
   *
   * @param model - countrySchema
   *
   * @beta
   */
  constructor(model: any) {
    super(model);
  }
}
