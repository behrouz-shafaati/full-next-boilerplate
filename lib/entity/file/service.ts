import c_service from '@/lib/entity/core/service';

export default class fileService extends c_service {
  /**
   * constructor function for fileService.
   *
   * @remarks
   * This method is part of the fileService class extended of the main parent class baseService.
   *
   * @param model - fileSchema
   *
   * @beta
   */
  constructor(model: any) {
    super(model);
  }
}
