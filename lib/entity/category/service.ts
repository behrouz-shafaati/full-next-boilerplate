import c_service from '@/lib/entity/core/service';

export default class categoryService extends c_service {
  /**
   * constructor function for categoryService.
   *
   * @remarks
   * This method is part of the categoryService class extended of the main parent class baseService.
   *
   * @param model - categorySchema
   *
   * @beta
   */
  constructor(model: any) {
    super(model);
  }
}
