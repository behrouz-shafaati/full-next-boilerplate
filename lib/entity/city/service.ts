import c_service from '@/lib/entity/core/service';

export default class cityService extends c_service {
  /**
   * constructor function for cityService.
   *
   * @remarks
   * This method is part of the cityService class extended of the main parent class baseService.
   *
   * @param model - citySchema
   *
   * @beta
   */
  constructor(model: any) {
    super(model);
  }
}
