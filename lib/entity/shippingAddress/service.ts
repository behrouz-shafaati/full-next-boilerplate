import c_service from '@/lib/entity/core/service';

export default class shippingAddressService extends c_service {
  /**
   * constructor function for shippingAddressService.
   *
   * @remarks
   * This method is part of the shippingAddressService class extended of the main parent class baseService.
   *
   * @param model - shippingAddressSchema
   *
   * @beta
   */
  constructor(model: any) {
    super(model);
  }
}
