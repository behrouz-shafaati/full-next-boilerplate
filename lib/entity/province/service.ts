import c_service from '@/lib/entity/core/service';

export default class Service extends c_service {
  /**
   * constructor function for Service.
   *
   * @remarks
   * This method is part of the Service class extended of the main parent class baseService.
   *
   * @param model - Schema
   *
   * @beta
   */
  constructor(model: any) {
    super(model);
  }
}
